import React from "react";
import { OptionsContainer, Title } from "./Options.styles";

const Options = () => {
	return (
		<OptionsContainer>
			<Title>Settings</Title>
		</OptionsContainer>
	);
};

export default Options;
