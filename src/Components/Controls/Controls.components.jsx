import React from "react";
import { ControlsContainer, Button } from "./Controls.styles";

const Controls = () => {
	return (
		<ControlsContainer>
			<Button>Start</Button>
			<Button>Reset</Button>
		</ControlsContainer>
	);
};

export default Controls;
